name: 03-workflow-runners

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: SHOW OS
        run: |
          echo "running on UBUNTU OS"
          echo "runner_os: $RUNNER_OS"
        
